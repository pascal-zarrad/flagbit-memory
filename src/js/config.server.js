/*

  Configuration of the Flagbit Memory websocket server.

     Change the below values of the SERVER_CONFIG constant to fit your needs.
    To change the configuration of the client, refer to config.client.ts

 */

/**
 * The configuration itself.
 * Change the values below to fit your needs!
 */
const SERVER_CONFIG = {
  PORT: 8080
};

/**
 * Export the config to make it available in other scripts.
 */
module.exports = SERVER_CONFIG;